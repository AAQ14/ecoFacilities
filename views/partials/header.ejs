<link rel="stylesheet" href="/style.css">
<nav>
    <% if(user) {%>
        <% if(user.userType === 'Admin') {%>
                <a href="/users">Users Info</a>
             <% } %>
    <a href="/ecoFacilities">Eco Facilities</a>
    <a href="/auth/logout">Logout</a>
    <%= user.username%>

   

    <%} else{%>
        <a href="/auth/login">Login</a>
        <a href="/auth/sign-up">Sign Up</a>
    <%}%>
</nav>